# Global plugin configurations

enabled: true                 # Set to false to disable this plugin completely

# Default options for DropBox Sync configuration.

app:
  key: "yourappkey"           # DropBox API key
  secret: "yourappsecret"     # DropBox API secret
  full_access: true                 # DropBox Accessibility

route: "/dropboxsync" 				# Route for sync
key: "yourstrongkey"  				# Key to use for sync access

# Global and page specific configurations